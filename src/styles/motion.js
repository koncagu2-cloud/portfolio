/** Snappy UI (buttons, nav) — still eased, not linear */
export const easeOutExpo = [0.16, 1, 0.3, 1]
export const easeInOutExpo = [0.7, 0, 0.2, 1]

/** Default motion — natural, less “template” */
export const easeOutSoft = [0.22, 1, 0.36, 1]

/** Slow, reflective surfaces (quotes, principles) */
export const easeCalm = [0.45, 0, 0.2, 1]

export const durations = {
  fast: 0.22,
  base: 0.48,
  slow: 0.72,
  breath: 0.95,
}

